import "../index.js";

describe("Backend Basic Test", () => {
  test("should load without crashing", () => {
    // If index.js loads without errors, this test will pass.
    expect(true).toBe(true);
  });
});